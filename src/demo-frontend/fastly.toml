# This file describes a Fastly Compute@Edge package. To learn more visit:
# https://developer.fastly.com/reference/fastly-toml/

authors = ["komuro@fastly.com"]
description = "Serves application static files and forwards other requests to the origin, upgrading applicable connections to WebSocket."
language = "rust"
manifest_version = 2
name = "demo-frontend"
service_id = ""
